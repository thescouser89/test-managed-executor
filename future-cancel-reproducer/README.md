This is to prove that we can't really cancel a future. We have to check if the
thread the future is running has been interrupted instead to know if we should
stop or not.

To test:
```
$ ./runme.sh
```
